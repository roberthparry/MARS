# MARS dval Lab Icon Concepts

Open `preview.html` in a browser to compare the concept icons side by side.

Once a concept is chosen, copy its SVG over `packaging/linux/mars-dval-lab.svg`.
The desktop launcher and the dval Lab browser tab both use that file.
