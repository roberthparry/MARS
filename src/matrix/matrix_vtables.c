#include "matrix_vtable_defs.h"

const struct store_vtable dense_store = {
    .create                   = store_create_dense,
    .alloc                    = dense_alloc,
    .free                     = dense_free,
    .get                      = dense_get,
    .set                      = dense_set,
    .swap_rows                = dense_swap_rows,
    .row_eliminate_from       = dense_row_eliminate_from,
    .materialise              = NULL,
    .is_sparse_storage        = dense_is_sparse_storage,
    .is_sparse_like           = dense_is_sparse_like,
    .is_diagonal              = dense_is_diagonal,
    .is_upper_triangular      = generic_is_upper_triangular,
    .is_lower_triangular      = generic_is_lower_triangular,
    .nonzero_count            = dense_nonzero_count,
    .elementwise_unary_store  = store_self_unary,
    .transpose_store          = store_self_transpose
};

const struct store_vtable sparse_store = {
    .create                   = store_create_sparse,
    .alloc                    = sparse_alloc,
    .free                     = sparse_free,
    .get                      = sparse_get,
    .set                      = sparse_set,
    .swap_rows                = sparse_swap_rows,
    .row_eliminate_from       = sparse_row_eliminate_from,
    .materialise              = sparse_materialise,
    .is_sparse_storage        = sparse_is_sparse_storage,
    .is_sparse_like           = sparse_is_sparse_like,
    .is_diagonal              = sparse_is_diagonal,
    .is_upper_triangular      = generic_is_upper_triangular,
    .is_lower_triangular      = generic_is_lower_triangular,
    .nonzero_count            = sparse_nonzero_count,
    .elementwise_unary_store  = store_self_unary,
    .transpose_store          = store_self_transpose
};

const struct store_vtable identity_store = {
    .create                   = store_create_identity,
    .alloc                    = ident_alloc,
    .free                     = ident_free,
    .get                      = ident_get,
    .set                      = ident_set,
    .swap_rows                = ident_swap_rows,
    .row_eliminate_from       = ident_row_eliminate_from,
    .materialise              = ident_materialise,
    .is_sparse_storage        = identity_is_sparse_storage,
    .is_sparse_like           = identity_is_sparse_like,
    .is_diagonal              = store_true,
    .is_upper_triangular      = store_true,
    .is_lower_triangular      = store_true,
    .nonzero_count            = identity_nonzero_count,
    .elementwise_unary_store  = identity_unary_store,
    .transpose_store          = store_self_transpose
};

const struct store_vtable diagonal_store = {
    .create                   = store_create_diagonal,
    .alloc                    = diagonal_alloc,
    .free                     = diagonal_free,
    .get                      = diagonal_get,
    .set                      = diagonal_set,
    .swap_rows                = diagonal_swap_rows,
    .row_eliminate_from       = diagonal_row_eliminate_from,
    .materialise              = diagonal_materialise,
    .is_sparse_storage        = diagonal_is_sparse_storage,
    .is_sparse_like           = diagonal_is_sparse_like,
    .is_diagonal              = store_true,
    .is_upper_triangular      = store_true,
    .is_lower_triangular      = store_true,
    .nonzero_count            = diagonal_nonzero_count,
    .elementwise_unary_store  = store_self_unary,
    .transpose_store          = store_self_transpose
};

const struct store_vtable upper_triangular_store = {
    .create                   = store_create_upper_triangular,
    .alloc                    = upper_triangular_alloc,
    .free                     = triangular_free,
    .get                      = upper_triangular_get,
    .set                      = upper_triangular_set,
    .swap_rows                = upper_triangular_swap_rows,
    .row_eliminate_from       = upper_triangular_row_eliminate_from,
    .materialise              = upper_triangular_materialise,
    .is_sparse_storage        = upper_triangular_is_sparse_storage,
    .is_sparse_like           = upper_triangular_is_sparse_like,
    .is_diagonal              = upper_triangular_is_diagonal,
    .is_upper_triangular      = store_true,
    .is_lower_triangular      = generic_is_lower_triangular,
    .nonzero_count            = upper_triangular_nonzero_count,
    .elementwise_unary_store  = store_self_unary,
    .transpose_store          = upper_triangular_transpose_store
};

const struct store_vtable lower_triangular_store = {
    .create                   = store_create_lower_triangular,
    .alloc                    = lower_triangular_alloc,
    .free                     = triangular_free,
    .get                      = lower_triangular_get,
    .set                      = lower_triangular_set,
    .swap_rows                = lower_triangular_swap_rows,
    .row_eliminate_from       = lower_triangular_row_eliminate_from,
    .materialise              = lower_triangular_materialise,
    .is_sparse_storage        = lower_triangular_is_sparse_storage,
    .is_sparse_like           = lower_triangular_is_sparse_like,
    .is_diagonal              = lower_triangular_is_diagonal,
    .is_upper_triangular      = generic_is_upper_triangular,
    .is_lower_triangular      = store_true,
    .nonzero_count            = lower_triangular_nonzero_count,
    .elementwise_unary_store  = store_self_unary,
    .transpose_store          = lower_triangular_transpose_store
};

static const struct elem_fun_vtable number_fun = {
    .exp = num_scalar_exp,
    .sin = num_scalar_sin,
    .cos = num_scalar_cos,
    .tan = num_scalar_tan,
    .sinh = num_scalar_sinh,
    .cosh = num_scalar_cosh,
    .tanh = num_scalar_tanh,
    .sqrt = num_scalar_sqrt,
    .log = num_scalar_log,
    .asin = num_scalar_asin,
    .acos = num_scalar_acos,
    .atan = num_scalar_atan,
    .asinh = num_scalar_asinh,
    .acosh = num_scalar_acosh,
    .atanh = num_scalar_atanh,
    .erf = num_scalar_erf,
    .erfc = num_scalar_erfc,
    .erfinv = num_scalar_erfinv,
    .erfcinv = num_scalar_erfcinv,
    .gamma = num_scalar_gamma,
    .lgamma = num_scalar_lgamma,
    .digamma = num_scalar_digamma,
    .trigamma = num_scalar_trigamma,
    .tetragamma = num_scalar_tetragamma,
    .gammainv = num_scalar_gammainv,
    .normal_pdf = num_scalar_normal_pdf,
    .normal_cdf = num_scalar_normal_cdf,
    .normal_logpdf = num_scalar_normal_logpdf,
    .lambert_w0 = num_scalar_lambert_w0,
    .lambert_wm1 = num_scalar_lambert_wm1,
    .productlog = num_scalar_productlog,
    .ei = num_scalar_ei,
    .e1 = num_scalar_e1
};

const struct elem_vtable number_elem = {
    .size            = sizeof(number_t),
    .kind            = ELEM_NUMBER,
    .public_type     = MAT_TYPE_NUMBER,
    .init_zero_slot  = num_init_zero_slot,
    .copy_value      = num_copy_value,
    .destroy_value   = num_destroy_value,
    .is_structural_zero = num_is_structural_zero,
    .add             = num_add_wrap,
    .sub             = num_sub_wrap,
    .mul             = num_mul_wrap,
    .inv             = num_inv_wrap,
    .conj_elem       = num_conj_elem,
    .zero            = &NUM_ZERO,
    .one             = &NUM_ONE,
    .cmp             = number_cmp_wrap,
    .print           = num_print_wrap,
    .format_scalar   = num_format_scalar,
    .fun             = &number_fun
};

static const struct elem_fun_vtable expr_fun = {
    .exp = expr_scalar_exp,
    .sin = expr_scalar_sin,
    .cos = expr_scalar_cos,
    .tan = expr_scalar_tan,
    .sinh = expr_scalar_sinh,
    .cosh = expr_scalar_cosh,
    .tanh = expr_scalar_tanh,
    .sqrt = expr_scalar_sqrt,
    .log = expr_scalar_log,
    .asin = expr_scalar_asin,
    .acos = expr_scalar_acos,
    .atan = expr_scalar_atan,
    .asinh = expr_scalar_asinh,
    .acosh = expr_scalar_acosh,
    .atanh = expr_scalar_atanh,
    .erf = expr_scalar_erf,
    .erfc = expr_scalar_erfc,
    .erfinv = expr_scalar_erfinv,
    .erfcinv = expr_scalar_erfcinv,
    .gamma = expr_scalar_gamma,
    .lgamma = expr_scalar_lgamma,
    .digamma = expr_scalar_digamma,
    .trigamma = expr_scalar_trigamma,
    .tetragamma = NULL,
    .gammainv = expr_scalar_gammainv,
    .normal_pdf = expr_scalar_normal_pdf,
    .normal_cdf = expr_scalar_normal_cdf,
    .normal_logpdf = expr_scalar_normal_logpdf,
    .lambert_w0 = expr_scalar_lambert_w0,
    .lambert_wm1 = expr_scalar_lambert_wm1,
    .productlog = expr_scalar_productlog,
    .ei = expr_scalar_ei,
    .e1 = expr_scalar_e1
};

const struct elem_vtable expr_elem = {
    .size            = sizeof(expr_t *),
    .kind            = ELEM_EXPR,
    .public_type     = MAT_TYPE_EXPR,
    .init_zero_slot  = expr_init_zero_slot,
    .copy_value      = expr_copy_value,
    .destroy_value   = expr_destroy_value,
    .simplify_value  = expr_simplify_value,
    .is_structural_zero = expr_is_structural_zero,
    .add             = expr_add_wrap,
    .sub             = expr_sub_wrap,
    .mul             = expr_mul_wrap,
    .inv             = expr_inv_wrap,
    .conj_elem       = expr_conj_elem,
    .zero            = &EXPR_ZERO,
    .one             = &EXPR_ONE,
    .cmp             = expr_cmp_wrap,
    .print           = expr_print_wrap,
    .format_scalar   = NULL,
    .fun             = &expr_fun
};
