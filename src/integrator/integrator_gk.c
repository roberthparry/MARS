#include "integrator_internal.h"

/*
 * The callback-based Gauss-Kronrod entry point was removed as part of the
 * expression-only integrator migration. This translation unit remains so the
 * build layout does not need to change while the rest of the integrator code
 * is cleaned up.
 */
